name: Build iOS IPA

on:
  workflow_dispatch:

  push:
    branches: [main]

jobs:
  build:

    runs-on: macos-latest
    steps:
      -
 uses: actions/checkout@v4
      - uses: maxi
m-lobanov/setup-xcode@v1
        with: { xcod
e-version: latest-stable }
      - run: chmod
 +x build_unsigned.sh build_esign_ready_ipa.s
h
      - run: ./build_unsigned.sh
      - ru
n: ./build_esign_ready_ipa.sh build/tefvx.ipa

      - uses: actions/upload-artifact@v4
   
     with:
          name: tefvx-ipa
        
  path: build/tefvx.ipa


